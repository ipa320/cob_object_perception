sensor_msgs/PointCloud2 pointcloud    # the original XYZRGB point cloud
Int32Array[] clusters     # clusters from segmentation (each cluster contains a vector of point indices belonging to that cluster)
